root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ."
  bin = "./tmp/main"
  include_ext = ["go", "mod", "toml"]
  exclude_dir = ["tmp", "data"]
  delay = 500

[log]
  time = false

[misc]
  clean_on_exit = true
